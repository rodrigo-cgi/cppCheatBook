#include <iostream>
#include "class.h" 

using std; // which means, no need to use the std:: namespace. To use std::string use simply string:
string no_namespace_needed;

int main(){


    return 0; // main must return 0 otherwise will return error
}